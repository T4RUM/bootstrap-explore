import { createContext } from "react";

// Define um contexto na aplicação. Um mecanismo para compartilhar dados entre/para todos os componentes da aplicação.

export const UsuarioContext = createContext();
